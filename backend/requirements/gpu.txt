# CUDA-only dependencies. DO NOT install on macOS — these require an NVIDIA GPU.
# Installed only on the Bengaluru 16GB box in Phase 9.
#
#   pip install -r requirements/gpu.txt --extra-index-url https://download.pytorch.org/whl/cu121
#
-r app.txt

# PyTorch (CUDA 12.1 build — install via the extra index URL above)
torch==2.5.1
torchvision==0.20.1

# Diffusion / model serving
diffusers==0.32.1
transformers==4.48.0
accelerate==1.2.1
safetensors==0.4.5
sentencepiece==0.2.0

# Memory optimization for 16GB VRAM (Phase 9 tuning)
bitsandbytes==0.45.0
xformers==0.0.28.post3

# AI Edit (E5) — object detection, segmentation, restoration.
#   GroundingDINO ships inside transformers
#   (AutoModelForZeroShotObjectDetection), already pinned above. SAM 2 and
#   Real-ESRGAN are added here:
realesrgan==0.3.0
basicsr==1.4.2
# SAM 2 has no PyPI release — install from GitHub on the box:
#   pip install "git+https://github.com/facebookresearch/sam2.git"
# ProPainter (optional, better video inpainting than per-frame SD) is also a
# GitHub repo; clone + install per its README if used during tuning:
#   https://github.com/sczhou/ProPainter

# Music generation (optional)
# audiocraft==1.3.0
